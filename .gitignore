# This repo is Python, TypeScript and Terraform. There is no .NET here, so
# there is no Visual Studio / MSBuild / NuGet boilerplate either.

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editors ---
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/
*.swp
*~

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/
htmlcov/
*.egg-info/

# --- Node / Vite ---
node_modules/
dist/
build/
.vite/
*.local
*.tsbuildinfo

# --- Environment and secrets ---
.env
.env.*
!.env.example
# Terraform apply output and any stray run log can carry resource ids.
*.log

# Denylist local override (never commit real terms)
services/api/denylist.local.txt

# --- Terraform ---
.terraform/
.terraform.lock.hcl.bak
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
!*.tfvars.example
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Eval results (never commit) ---
evals/results/

# --- Screenshot redaction config (contains real tenant identifiers) ---
scripts/portal_redact.config.json
!scripts/portal_redact.config.example.json
# Generated capture bundle: inlines the config above, so it holds real values.
scripts/.portal_redact.bundle.js

# --- Foundry bindings (env-specific, never commit) ---
.foundry/agent-bindings.local.json

# --- Generated knowledge export (Module 6) ---
evals/knowledge/
