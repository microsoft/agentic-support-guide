# Leave unset. The browser calls /api on the web tier's own origin and
# server.js proxies it to the API with the shared key attached. Setting this
# to the API hostname bypasses the proxy, and the API will reject the call.
# VITE_API_BASE_URL=/api
